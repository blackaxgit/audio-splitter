apiVersion: v1
kind: Service
metadata:
  name: {{ include "audio-splitter.fullname" . }}
  labels:
    {{- include "audio-splitter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "audio-splitter.selectorLabels" . | nindent 4 }}
